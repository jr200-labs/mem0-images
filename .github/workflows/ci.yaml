name: test

on:
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

jobs:
  commitlint:
    uses: jr200-labs/github-action-templates/.github/workflows/commitlint.yaml@master

  lint-workflow-runners:
    uses: jr200-labs/github-action-templates/.github/workflows/lint_workflow_runners.yaml@master

  lint-pr-metadata:
    uses: jr200-labs/github-action-templates/.github/workflows/lint_pr_metadata.yaml@master
